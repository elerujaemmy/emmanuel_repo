version: 0.2

phases:
  install:
    commands:
      - pyenv install poetry
      - poetry install
  pre_build:
    commands:
      - poetry run pytest
  build:
    commands:
      - poetry build
artifacts:
  files:
    - packaged.yaml
  discard-paths: yes
